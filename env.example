# 환경변수 설정 예시 파일
# 실제 배포시에는 이 파일을 복사하여 .env 파일을 만들고 실제 값으로 교체하세요.

# 데이터베이스 설정
DB_URL=jdbc:mysql://localhost:3306/mini_carrot_product?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
DB_USERNAME=root
DB_PASSWORD=your_mysql_password

# JWT 설정 (User Service와 동일한 키 사용 필요)
JWT_SECRET=minicarrotusersecretkeyforjwttoken123456789012345678901234567890
JWT_EXPIRATION=86400000

# JPA 설정
JPA_DDL_AUTO=update
JPA_SHOW_SQL=true

# 로깅 레벨
LOG_LEVEL=DEBUG

# 배포 환경 (local, dev, prod)
SPRING_PROFILES_ACTIVE=local

# 서버 포트
SERVER_PORT=8082

# 파일 업로드 설정
MAX_FILE_SIZE=10MB
MAX_REQUEST_SIZE=10MB 